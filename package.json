{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=14 <=18"
  },
  "dependencies": {
    "axios": "^0.21.4",
    "buffer": "^6.0.3",
    "quill": "^1.3.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-hook-form": "^7.32.2",
    "react-router-dom": "^5.3.0",
    "react-scripts": "5.0.1",
    "react-timer-hook": "^3.0.5",
    "react-toastify": "^9.1.1",
    "simple-peer": "^9.11.1",
    "socket.io-client": "^4.2.0",
    "use-sound": "^4.0.1",
    "uuid": "^8.3.2",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "start": "export NODE_OPTIONS=--openssl-legacy-provider && npx --yes react-scripts start",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && npx --yes react-scripts build",
    "test": "export NODE_OPTIONS=--openssl-legacy-provider && npx --yes react-scripts test --env=jsdom",
    "eject": "npx --yes react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
